{"Activity":
    [
    {% for entity in entities %}
        {% include 'ItoApiBundle:Activity:show.json.twig'  with { 'entity': entity }%} 
        {% if not loop.last %} , 
    {% endif%}{% endfor %}
    ]
}