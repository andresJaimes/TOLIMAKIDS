{"Places":
    [
    {% for entity in entities %}
        {% include 'ItoApiBundle:Places:show.json.twig'  with { 'entity': entity }%} 
        {% if not loop.last %} , 
    {% endif%}{% endfor %}
    ]
}